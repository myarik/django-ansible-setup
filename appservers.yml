---
  - hosts: webservers
    become: yes
    roles:
      - { role: common,
          tags: [webservers, setup_common_pkg]
      }
      - { role: app,
          tags: [webservers, setup_app]
      }
